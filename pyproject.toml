[project]
name = "koleth-ai-pulse"
version = "0.1.0"
requires-python = "~=3.12.0"
dependencies = [
    "fastapi==0.116.1",
    "feedparser==6.0.11",
    "psycopg[binary]>=3.2,<4",
    "PyYAML==6.0.2",
    "requests==2.32.4",
]

[tool.vercel]
entrypoint = "index:app"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
