# Hourly collector run for a host installation.
# Edit paths to match your clone and virtualenv.
0 * * * * cd /opt/koleth-ai-pulse && /opt/koleth-ai-pulse/.venv/bin/python -m collector.collect --config config/sources.yaml >> /var/log/koleth-ai-pulse-collector.log 2>&1
